paths=(/usr/local/bin /usr/local/sbin /usr/local/opt/ruby/bin /usr/local/share/npm/bin ~/.cabal/bin ~/bin)

for p in ${paths[@]}; do
    if [[ -d $p ]]; then
        export PATH="$p":"$PATH"
    fi
done

unset paths
